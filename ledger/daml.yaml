# VERIFY before building:
#   - sdk-version: set to cn-quickstart's DAML_RUNTIME_VERSION (check quickstart/.env).
#   - script lib may be `daml-script` or `daml3-script` on the 3.x line; `import Daml.Script` is stable.
sdk-version: 3.3.0-snapshot.20250930.0
name: atrium
version: 0.1.0
source: daml
dependencies:
  - daml-prim
  - daml-stdlib
  - daml-script
