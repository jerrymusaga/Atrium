{
  "name": "atrium-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Atrium executor app — holds the operator party, talks to the Canton JSON Ledger API, orchestrates the AllocationV1 close.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node --loader tsx src/server.ts",
    "dars": "tsx scripts/dars.ts"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.3"
  }
}
